import { CertificadoDigital } from './certificadodigital';

describe('Certificadodigital', () => {
  it('should create an instance', () => {
    expect(new CertificadoDigital()).toBeTruthy();
  });
});
